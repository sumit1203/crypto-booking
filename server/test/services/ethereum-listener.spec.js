/* eslint-env mocha */
require('dotenv').config({ path: './test/utils/.env' });
// const { expect } = require('chai');
// const { onBookingDoneData } = require('../../src/services/ethereum-listener');

describe('Secret codes', () => {
  xit('Should send a correct formated email');
});
